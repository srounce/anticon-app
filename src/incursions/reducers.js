import { combineReducers } from 'redux'

import { LOCATION_UPDATE } from '../geolocation/actions'
import * as ActionTypes from './actions'

const incursions = (state = [], action) => {
  switch (action.type) {
  }

  return state
}
